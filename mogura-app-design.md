# さかなたたきアプリ「ぽこ！」設計書 v0.5

対象: Claude Code による実装（この1ファイルだけで実装開始できることを目標）
前提: 1歳半（現在1歳7ヶ月）の子どもが自分で触って遊ぶ。保護者が横にいなくても壊れない
前身: いないいないばあアプリ「ばあ！」（`peek-aboo`）。**背景・core/ui はそのまま引き継ぐ**
祖先: 水族館アプリ「みずのなか」（`suizokukan`）

## v0.2 で人間が決めたこと（2026-09-13）

v0.1 は「もぐら」を叩く設計だった。実機を使う本人の好みに合わせて、次のとおり変える。

| | v0.1 | **v0.2** | なぜ |
|---|---|---|---|
| 出てくるもの | もぐら（動物25体）| **さかな** | **本人が魚を好きだから。** 好きなものが出てくること自体が、この月齢では最大の動機になる |
| 場面 | 7つ | **2ステージ**（いけ・うみ）| 場面が多いことに価値は無いと分かった。**切り替えずに遊び続ける**ほうがこの月齢には合う |
| 出てくる速さ | `rising` 0.22秒・`up` 2.6秒 | **`rising` 0.65秒・`up` 4.0秒** | 1歳半は狙って当てられない。**ゆっくり出てゆっくり待つ**ほうが当たる（§4-7 の介助と同じ考え） |
| 声 | 出るときは無言、叩くと動物の鳴き声 | **出るとき「ばあっ！」／叩くと「いてっ」** | 「ばあ！」で録った「ばあっ！」がそのまま使える。**出た・当たったの2つが声で分かれる** |
| 魚の数 | 8種（1ステージ4種）※v0.2 初稿 | **4種（1ステージ2種）** | **1ステージに出るキャラは2体まで**。種類が多くても、この月齢では見分けきれない。**2種なら「どっちが出た」が分かる** |
| 同時に出る数 | 1〜3匹 ※v0.2 初稿 | **1〜2匹** |
| 水たまりの数 | 4箇所 ※v0.2 初稿 | **6箇所** | 人間が決めた（2026-09-13）。押せるところが多いほうが1歳半には当たる。2列×3行 | 上と同じ話。1ステージ2種で**同じ魚を同時に2箇所へ出さない**ので、上限は自動的に2匹になる |
| 大きい魚 | 8回に1回 ※v0.2 初稿 | **5回に1回** | 人間が決めたレベル |

**「ゆっくり出す」と「0フレームで反応する」は矛盾しない**（§4-3 に書いた）。
遅くするのは**魚が出てくる動き**であって、**叩いた瞬間の反応ではない。**

## v0.3 で人間が決めたこと（2026-09-13、実機で触ってから）

Phase 3 まで作って実機で触ってもらった。**2回目を叩いた**（＝叩くこと自体は
刺さった）。速さもこのままでよい。そのうえで4点の指示。

| | v0.2 | **v0.3** | なぜ |
|---|---|---|---|
| 穴の向き | 横長（見下ろした池）| **縦長** | 「魚の出方（見え方）があまり良くない」。見下ろし 11.8° では池が潰れて見えるので、上下の動きが「水から出てきた」に見えない |
| 出る向き | 下から上へ浮く | **左（穴）から右へ泳ぎ出る** | 横向きなら頭から尾までの輪郭がそのまま見えるので、出てきたことが一目で分かる |
| 声と姿の順番 | 同時 | **声が先、姿はあと**（`calling` 0.38秒）| 「『ばあっ』する前にチラッと見えている」。**何も見えない状態で「ばあっ」と言って出てくる** |
| 叩いた演出 | 潰れ・しぶき・ゆれ | **＋ピコピコハンマー＋たんこぶ** | 叩いたことが絵で分かるほうが、1歳半にも因果が読める |

**隠すのは板ではなく切り取り**（`renderer.localClippingEnabled`）。
穴の口より左は描かないので、**隠れているあいだ1画素も見えない。**
縦長の穴を板で覆うと、覆う板のほうが大きくなって背景を隠す。

**当たり判定は「いま魚が居るところ」で見る**（`HoleSystem.measure` の `offsetX`）。
穴の中心のままだと、魚が右へ泳ぎ出たぶん右隣の穴に近づいて、
**魚を叩いたのに隣の空振りになる**回ができる。

実測の当たり半径（魚が出きったとき）: Pixel 7 **76.6px** / iPhone 12 **68.4px** /
360×600 54.5px / 横持ち 35.1px。**魚は約 100×40px あるので、
体のどこを押しても中心から 76px に入る。**

---

## v0.4 で人間が決めたこと（2026-09-14、2回目の実機）

3点の指示。

| | v0.3 | **v0.4** | なぜ |
|---|---|---|---|
| 大きい魚 | よく出る | **レアでよい** | 「大きい魚の出現頻度が高い」。**特別な仕掛けは無かった** —— 大きく見えていたのは こい そのもので、体高でそろえていたせいで細長い魚だけ 1.5倍の面積になっていた。見かけの面積（`TARGET_AREA`）でそろえて消した |
| さかなの絵 | canvas の手描き | **「みずのなか」の 3D モデル** | 「魚のクオリティーが低すぎます」 |
| 背景 | 手続き生成（野原→水中）| **「みずのなか」の実写ループ動画** | 「背景が野原で魚とマッチしていない」 |

**「以前に作成した suizokukan の3D魚や背景の動画をそのまま使用してください」**（人間の指示）。

### 素材は3体しかない

`public/models/` にあるのは **クマノミ・琉金・エイの3体**。2ステージ × 2種 =
4枠には足りないので、**クマノミのモデルを2枠で使い、体の絵（`skinUrl`）を
変えて別の魚にする**（かわ = はぎ／うみ = くまのみ）。

**同じステージには置かない。** 輪郭が同じ2匹が並ぶと、色を変えても
「同じ魚を2色に塗った」に見える。ステージをまたぐぶんは同時に見ないので許す。
単体テストが見張っている。

### 正規化は「みずのなか」のものをそのまま持ってくる

`src/poko/ModelGeometry.ts`（`normalizeModelGeometry` / `tintModelGeometry`）は
向こうの `creatures/ProceduralFish.ts` からの移植。**自前で書き直して2回失敗した。**

- **模様が出ない。** 配布モデルは UV を持たないことが多く、`material.map` に
  体の絵を貼っても全頂点が uv(0,0) を読む。実機では「濃い茶色の魚」になった。
  正規化のほうが **UV を作る**（u = 頭0→尾1 / v = 腹0→背1）
- **エイが黒い影にしか見えない。** モデルのマテリアル色（暗い青灰）が
  頂点色に焼き込まれるので、`color` を明るくしても何も変わらない。
  `tintModelGeometry` が「**陰影はモデルから・色は設定から**」塗り替える
- **絵を貼るときは頂点色を白に戻す。** 戻さないと焼き込んだ色と二重に掛かって
  濁る。はぎ が**ほぼ黒い茶色**に見えていた直接の原因

### 向きの値だけは、写さずに毎回見ること

「みずのなか」の `modelFlip` / `modelRollDeg` をそのまま写すと、**3体のうち
2体が逆や横倒しになった**。向こうの魚は横向きに泳ぎ、この app の魚は
カメラの正面へ顔を出すので、体軸まわりの向きが違う。

| | みずのなか | **ぽこ！** | 絵で確かめたこと |
|---|---|---|---|
| 琉金 | roll 90 | roll 90 | そのまま合う |
| クマノミ | flip true | **flip なし** | 写すと尾から出てくる |
| エイ | 回転なし | **flip + roll 90** | 回さないと厚み 0.18 の板を真横から見る（線にしか見えない） |

CLAUDE.md「モデルを追加したら必ず動かして向きを見る」。**自動判定はしない。**

### 動画の上に板を置かない

背景は `<video>` を全画面に敷き、**透過した WebGL キャンバス**を重ねて見せる
（`scene/VideoLayer.ts`。みずのなかからの移植）。

**動画があるステージでは、地の板（16×16）を1枚も置かないこと。**
置くと動画が丸ごと隠れる。実際に隠れていて、画面には空のグラデーションしか
映っていなかった。**読み込みも再生も成功している**（`RES 206 river.webm` /
`readyState 4` / `paused false`）ので、**動画の側を疑っても永遠に見つからない。**

mp4 が読めない環境（オープンソース版 Chromium には H.264 が入っていない）では
webm に落ちる。**どちらも読めなければ手続き生成の絵に落ちる**（不変条件7）。

---

## v0.5 で人間が決めたこと（2026-09-14）

**「画面の上が空いているので岩を設置して、タップすると魚が前に
突き出してくるように（ばあっ！）してください。suizokukan の下にある
岩陰のばあの機能です」。**

詳細は §4-8。要点だけ:

| | v0.4 | **v0.5** |
|---|---|---|
| 画面の上（35%） | 何も無い | **岩を2つ置く**（上端は画面の外で切る） |
| 岩を押すと | — | **魚が手前へ突き出してくる**（「ばあっ！」） |
| 待っているあいだ | — | **岩の中に隠れている**（深度で隠す。消すだけにしない） |
| 岩の魚 | — | **水たまりに出ない種**（かわ = エイ／うみ = 琉金） |

---

## 0. この文書の位置づけ

**「ばあ！」は実機で受けなかった。** それが、この設計のいちばん重要な入力になる。

> 実機でのフィードバック（2026-09-13）:
> **「1、2回触るも『ばあっ』に対して全く面白さを感じていない」**

ここから読めることは1つで、**触らなかったのではない**ということ。
指は動いた。画面のどこを押せばいいかも分かった。分からなかったのは
**「押した結果」に価値がある**ということのほうだった。

「ばあ！」は、押すと**物が現れる**アプリだった。現れた動物はその場に立ち、
1.6秒たつとまた引っ込む。**押したことで世界が変わらない。**
この月齢の子が繰り返すのは、積み木を倒す・コップを落とす・紙を破るといった
**自分の一撃で物の形が変わる**遊びで、「現れる」はそこに入っていない。

だから今回は、**出現ではなく打撃**をコアに置く。

| 「ばあ！」で起きたこと | 「ぽこ！」での要件 |
|---|---|
| 押すと動物が**現れる**。何も壊れない | **押すと魚が潰れて沈む。** 形が変わる（§4-3） |
| 押してから出るまで **0.35〜0.50秒の「ため」** | **ためを持たない。** 押したその場で潰れる（§4-3 の0フレーム原則） |
| **1体ずつしか出さない**（人間が決めた） | **同時に複数出す。** 叩く相手が画面から消えない（§4-2） |
| 押すまで画面が**止まっている** | **押さなくても動き続ける。** 魚が勝手に出入りする（§4-2） |
| 隠れている＝**体の一部だけ見せる**（ヒント） | **全身を出す。** 隠す必要がない（§2 不変条件3 を削除） |
| 結果が「声」だけ | **音・変形・星・画面のゆれ・得点の5つが同時に返る**（§4-4） |
| 「ばあっ」が**押した結果**だった | **「ばあっ」は魚が出た合図、「いてっ」が叩けた合図。** 2つの出来事が声で分かれる（§4-4） |

**この表が守られているかだけを見れば、設計が崩れていないか分かる。**

---

## 1. アプリの狙い

### 1歳半にとっての「たたく」

1歳半〜2歳は、**因果の一撃**が分かりはじめる時期。
「自分がやった → 物が変わった」が短いほど、繰り返す。

設計上、これは3つの意味を持つ。

1. **間を置かない。** 押してから結果までの時間が長いほど、因果が切れる。
   「ばあ！」の 0.35秒の「ため」は、いないいないばあには必要だったが、
   たたきには**害しかない**
2. **結果は目に見える形の変化であること。** 音だけ・光だけでは弱い。
   **潰れる・沈む・飛ぶ**という、形そのものの変化が要る
3. **失敗を作らない。** 叩けなくても、外しても、何も減らない。
   1歳半は狙って当てられないので、**当たらないことを罰しない**

### 繰り返しは仕様であって、飽きではない

これは「ばあ！」から引き継ぐ。同じ水たまりを20回叩く。だから、
- 20回目も1回目と同じ品質で反応すること（不変条件2）
- ただし**中身は少し変わる**と、さらに続く（§6）

### 出てくるのは、ぜんぶ魚

**本人が魚を好きだから。**（2026-09-13、人間が決めた）

**種類が主役、という「ばあ！」の判断は引き継がない。**
出すのは**魚4種だけ**で、**1ステージに2種**しか出さない（§5-1）。
25体出した「ばあ！」で、種類の多さが面白さにつながった形跡が無かったため。

- **魚は手続き生成で作る**（`ProceduralFish.ts`）。切り抜きの絵は要らない。
  「ばあ！」の `public/animals/` 25枚は**使わない**（陸の動物なので）
- 見分けは**色ではなく輪郭**で付ける。「みずのなか」で
  「体型を指定しないと、チョウチョウウオもメダカも同じ魚になった」と実測している。
  `FishConfig` に体高・体幅・ひれ・尾・模様を持たせ、**データを読めば輪郭が分かる**ようにする
- 絵を置きたくなったら、`public/fish/<id>.webp` に置けば
  `CutoutAnimal`（道A）が拾う。**無くても動く**（不変条件7）

**魚は水の中のものなので、「穴」は水になる**（§3-2・§5-2）。

### 「ぽこ！」という名前

**水面に魚が出る音であり、叩いた音でもある。**
もぐらだったときは叩く音だけだったが、魚にしたことで
「ぽこっ」が出るときと叩くときの両方に効くようになった。

---

## 2. 幼児向け必須要件（不変条件）

**この章だけは実装より優先する。** 食い違ったら実装を直す。
「ばあ！」の不変条件を引き継ぎ、**変えたところを明示する**。

1. **画面のどこをタップしても必ず何かが起きる。** 無反応を作らない。
   魚の上なら潰れる。水たまりの上ならしぶきが跳ねる。それ以外でも波紋と音が出る
2. **どの瞬間に押しても反応する。** 出ている途中・引っ込む途中・潰れている最中でも
   受け付ける。**「アニメーション中だから無視」は禁止**
3. ~~隠れている動物は必ず体の一部が見えている~~
   → **削除。** 魚は自分から全身を出すので、ヒントの概念が無くなった
3b. **魚の居ない水たまりを叩いても、必ず反応する。**
   しぶきが跳ね、音が鳴る。**得点は増えない。声は出さない**（§4-5）
4. ~~登場は 0.35秒以内に始まる~~
   → **「叩いた反応は同じフレームで始まる」に置き換える**（§4-3）。
   0.35秒ではなく**0秒**。ためを作らない
4b. **連打しても、1回ごとに反応が返る**（不変条件2の具体化）。
   ただし**得点は「出ている魚を叩いたとき」しか増えない**。
   潰れているものを連打しても増えない（§4-6）
4c. **叩ける相手が画面から途切れない。** 最低1匹は常に出ているか、
   出はじめている（§4-2）。**画面に何も無い時間を作らない**
4d. **出てくる動きは遅くてよいが、叩いた反応は遅らせない**（§4-1・§4-3）。
   `rising` 0.65秒・`up` 4.0秒は**人間が決めた値**。勝手に速くしない
5. **スワイプ・ピンチ・ダブルタップ・長押しに機能を割り当てない。**
   例外はペアレンタルゲートの2秒長押しのみ（画面右上 24×24px）
6. **1秒に3回を超える明滅を作らない。** 急激な全画面フラッシュ禁止。
   画面のゆれは**叩いた水たまりのまわりだけ**。`prefers-reduced-motion` では弱める。
   **水面の波も 3回/秒 を超えない**
7. **素材ファイルが1つも無くてもアプリは必ず起動する。**
   `public/` を空にしても `npm run dev` が動くこと
8. **ステージ切替時に前のステージの geometry / material / texture を dispose する**
9. **音は初期ミュート。** 最初のタップで解禁し、フェードインさせる
10. **広告・外部リンク・課金・テキスト入力欄を子ども向け画面に置かない**
11. **得点は減らない。終わらない。制限時間を作らない**（§4-6）
11b. **「いてっ」を痛そうにしない**（§4-4）。おどけた驚きにする。
    涙・青ざめ・×印の目を作らない。**叩くことを悪いことに見せない**
12. **自動マージしない。** マージ判断は常に人間が行う。
    CI から Claude を自動で動かす仕組みも入れない（従量課金になるため）

### 不変条件は数値のテストで押さえる

「壊れても静かに壊れる」ものは目で見て気づけないので `tests/unit/safety.test.ts` に置く。
**このファイルが落ちたら、設計書ではなく実装を直すこと。**

- 60Hz で連打しても、1回ごとに `true` が返る
- 叩いた**同じ更新ステップ**で、潰れの進みが 0 より大きい（0フレーム原則）
- 得点は単調増加で、連打では増えない
- どの時刻でも、叩ける相手（`up` か `rising`）が 0 にならない
- 明滅が 3回/秒 を超えない
- 水たまりどうしの当たり判定の円が重ならない（端末4種で実測）
- **`rising` に入ったフレームでだけ「ばあっ」が鳴る**（`hidden` では鳴らない）
- **「いてっ」は得点が増えた回にだけ鳴る**（空振りと連打では鳴らない）
- `up` の長さが介助で 3.2〜5.2秒の外へ出ない

---

## 3. 画面と操作

### 3-1. レイアウト

「ばあ！」の 2.5D をそのまま引き継ぐ。カメラは `(0, 0.3, 7.2)` 固定、
縦持ちは fov 66°・アスペクト 0.49。

```
┌──────────────┐
│ ★★★☆☆☆☆☆☆☆  │  得点（絵のみ。数字を使わない）高さ 40px 以内
│              │
│   〈背景の絵〉  │  peek-aboo の絵から2枚だけ使う
│              │
│   ◠     ◠    │  奥の水たまり（2つ）
│              │
│  ◠       ◠   │  手前の水たまり（2つ）
│              │
└──────────────┘
```

### 3-2. 水たまりは地面に置く（「ばあ！」からの最大の変更）

「ばあ！」の隠れ場所は `y = +1.90 / −2.00` の 2×2 で、**上の段が空に浮いていた**。
見た目の判定（`visual-judge`）が4場面で繰り返し −12 を付けた欠点で、
最後まで解消できなかった（`peek-aboo/docs/visual-log.md` の「人間に決めてほしいこと」）。

**たたきゲームは、これが構造的に直る。** 魚が出てくる水たまりは地面にあるものなので、
背景の絵の**地平線より下**にしか置かない。

- 水たまりの位置は**ステージごとにデータで持つ**（`HoleConfig.position`）
- 初期値は背景の絵の地平線から決める。
  `scene/Backdrop.ts` の `sampleBackdropHorizon()` が既にあるので、それを使う
- 実測（peek-aboo で測った地平線の v。0 が上端、1 が下端）:

| 場面 | 地平線 v | 段差 | 備考 |
|---|---|---|---|
| おうち | 0.563 | 20.2 | 壁と床の境目 |
| そと | 0.333 | 17.7 | |
| のうじょう | 0.396 | 24.1 | |
| きょうりゅう | 0.542 | 20.1 | |
| うみ | — | 5.9 | **地面が無い絵。** 段差が小さく検出できない |
| どうぶつえん | — | 12.1 | 同上（閾値 14 に届かない） |
| のはら | — | 10.9 | 同上 |

**使う2場面はどちらも自動検出が効かない**（のはら 10.9・うみ 5.9 で、閾値 14 に届かない）。
**手で数値を入れる。** 自動検出に頼らない（「置いたのに見えない」を作らないため）。
初期値は **のはら 0.42 / うみ 0.55**（絵を見て決め、実機で詰める）。

水たまりを地面の帯に収めると縦の間隔が縮むので、**当たり判定の重なりを必ず測る**
（§3-3）。奥の段を小さくする遠近は**付けない** —— 小さいと叩きにくく、
1歳半に不利になる。同じ大きさのまま、位置だけ下げる。

### 3-3. 当たり判定は 3D のレイではなく画面座標で

「ばあ！」の実測をそのまま引き継ぐ。`ScreenProjector.distancePx()` で判定する。

**半径は定数では決められない。** ワールド座標を固定したまま画面の大きさだけ
変わるので、どんな定数を選んでも全端末では成立しない。

| 端末 | 「ばあ！」で測ったいちばん近い組 | 120+120 に対して |
|---|---|---|
| Pixel 7 412×839 | 204.8px | 35.2px 食い込む |
| iPhone 12 390×750 | 183.0px | 57.0px 食い込む |
| 360×600 | 146.4px | 93.6px 食い込む |
| 844×390（横持ち） | 118.7px | 121.3px 食い込む |

`HoleConfig.hitRadiusPx` は**上限**として持ち、`HoleSystem.radiusAt()` が
隣との距離を見て縮める。**下限は設けない**（下限が効いた瞬間に円が重なり、
「押したのに隣が反応する」が復活する）。

**水たまりを地面に寄せたぶん、「ばあ！」より縦の距離が縮む。** 置いたら必ず測ること。
「ばあ！」の 2×2 は実測で Pixel 7 でも 2.0px しか空いていない。
地平線より下に4つ収めると**確実に重なる**ので、
`radiusAt()` が縮めた結果が 70px を下回るなら、**4つを 3つに減らすほうを選ぶ**
（小さい当たり判定より、数が少ないほうが1歳半には当たる）。

### 3-4. 得点表示が当たり判定を塞がないこと

「みずのなか」で実際に起きた事故。切替ボタンを5個並べたら 390px 幅の端末で
2行に折り返し、バーの高さが 172px になって画面下部の当たり判定に覆いかぶさった。

得点は**画面最上部**に置く。水たまりは地平線より下（画面の下半分）にあるので、
上に置けば重ならない。それでも **E2E で「得点表示の下に当たり判定が無いこと」を見る**。

### 3-5. 操作はタップだけ

不変条件5。スワイプ・ピンチ・長押しに機能を割り当てない。
**「叩く」はタップ1回**で、押し込みの深さも速さも見ない。

---

## 4. コアループ（このアプリの心臓部）

### 4-1. さかなの状態遷移

```
hidden ──(抽選)──> rising ──> up ──┬──(叩かれた)──> hit ──> hidden
                     │             │
                     │             └──(時間切れ)──> retreating ──> hidden
                     │
                     └──(叩かれた)──> hit ──> hidden
```

| 状態 | 長さ | 中身 |
|---|---|---|
| `hidden` | 抽選まで | 水たまりだけが見えている |
| `rising` | **0.65秒** | 水から出てくる。**この間も叩ける**。出はじめに「ばあっ！」 |
| `up` | **4.0秒**（やさしい版の既定） | 出きって、ゆらゆらしている |
| `hit` | **0.45秒** | 潰れて水に沈む。**「いてっ」** |
| `retreating` | **0.50秒** | 叩かれずに自分で沈む |

**`rising` の途中でも叩ける**こと。出きるまで待たせない（不変条件2）。

#### なぜ「ゆっくり」なのか（2026-09-13、人間が決めた）

v0.1 は `rising` 0.22秒・`up` 2.6秒だった。**1歳半は狙って当てられない。**
指を画面へ運ぶまでに1秒近くかかるので、2.6秒では
「見つける → 運ぶ → 当てる」が間に合わない回が出る。

- `rising` **0.65秒**: 出てくる動きそのものが見える長さ。
  ここが速いと「いつのまにか居た」になって、**出た瞬間を見逃す**
- `up` **4.0秒**: 運んで当てるのに要る時間。§4-7 の介助はこの値を動かす

**この「ゆっくり」は §4-3 の0フレーム原則と衝突しない。**
遅くしているのは**魚が勝手に出てくる動き**で、
**叩いた瞬間の反応は 1フレームも遅らせない。**
「ばあ！」で害になった「ため」は、**押してから結果までの間**のことだった。

### 4-2. 出現の抽選（`Spawner`）

**押さなくても画面が動き続ける**のが、「ばあ！」との決定的な違い。

- 水たまりは4箇所。**同時に出ているのは 1〜2匹**（2026-09-13、人間が決めた）。
  1ステージに魚は2種しか居らず、**同じ魚を同時に2箇所へ出さない**ので、
  **上限 2匹は数の制限であると同時に、種類の制限でもある**
- **最低1体は常に出ているか、出はじめている**（不変条件4c）。
  0体になりそうなら、次の抽選を待たずに即座に1体出す
- 次の出現までの間隔: **0.8〜2.0秒**（やさしい版）。
  v0.1 の 0.5〜1.4秒から伸ばした —— `up` を 4.0秒にしたので、
  そのままだと常に上限まで出ていて画面が忙しくなる。
  **上限 2匹に達しているあいだは抽選そのものを止める**（間隔を数えない）
- **同じ水たまりから続けて出さない。** 直前に使った場所を除外する
  （「ばあ！」の移動モードで、同じ場所への即戻りを 20周 0回にした抽選をそのまま使う）
- 出る魚は、**そのステージの2種**から選ぶ。**同じ魚が同時に2箇所に出ない**

**乱数は独立したシードから引く。** three は `generateUUID()` で
1オブジェクトにつき `Math.random()` を4回消費するので、共有の乱数を使うと
オブジェクトを1つ足しただけで抽選が変わる（みずのなかで実測）。

### 4-3. 叩いた瞬間（0フレーム原則）

**このアプリでいちばん重要な規則。**

> タップを受け取った**その場**で `state = 'hit'` にし、
> **次の更新ステップを待たずに**音・得点・星・ゆれを出す。

「ばあ！」は押してから 0.35秒後に山が来る作りで、そこが受けなかった。
**ここに「ため」を1フレームも入れてはいけない。**

実装上の注意:
- `Input` のイベントハンドラの中で `MoleSystem.hit()` を呼び、
  **その中で潰れの進み（`squash`）を 0 より大きくする**。
  次の `update()` を待って 0 のままにしないこと
- 単体テストが `hit()` の直後に `squash > 0` を見る
- `Loop` は 1/60 の固定タイムステップなので、画面に出るのは最大 16ms 後。
  **それ以上の遅れを設計で足さない**
- **「ゆっくり出す」（§4-1）をここに持ち込まないこと。** `rising` を 0.65秒に
  したのは出てくる動きの話で、`hit` に入る速さとは無関係。
  `hit()` の中で `squash` を上げるのは**即時**

### 4-4. 手ごたえは5つ同時に返す

1つでは弱い。「ばあ！」は声だけだった。

| 何が | どう | 制約 |
|---|---|---|
| **形** | 縦に 0.45 まで潰れ、水に沈む | 潰れは 0.12秒で最大、そこから沈む |
| **音** | 「ぽこっ」（水の音）＋**声「いてっ」** | `AudioBus.playOneShot()` と `playVoice()`。**`speak()` を使わない**（下を読むこと） |
| **しぶき** | 叩いた地点から水しぶき 6〜8粒、外へ飛んで消える | 0.5秒で消える。`HitEffect` |
| **ゆれ** | **叩いた水たまりのまわりだけ** 2.5° を 0.15秒 | 全画面を揺らさない（不変条件6） |
| **得点** | ★が1つ増える | §4-6 |

**全画面フラッシュは禁止**（不変条件6）。光らせたいなら、
叩いた地点のしぶきの明るさで表現する。

#### 声は2本だけ。役割を混ぜない（2026-09-13、人間が決めた）

| いつ | 何と言う | 誰の合図か |
|---|---|---|
| `rising` に入った瞬間 | **「ばあっ！」** | **魚が出た**（子どもはまだ何もしていない） |
| 叩かれた瞬間（`hit`） | **「いてっ」** | **当たった**（子どもがやった） |

**この2つを取り違えないこと。** 「ばあ！」で実際に踏んだ間違いが2つある。

1. **隠れたままなのに「ばあっ」と言った。** 何も出ていないのに出た合図が鳴ると、
   いちばん紛らわしい。**「ばあっ」は魚が水面から見えはじめるフレームでだけ鳴らす**
2. **`speak('あれ？', 'baa')` と書くと、録音してある「ばあっ！」が鳴った。**
   第1引数の文字は読み上げが使えるときにしか使われない

**`AudioBus.speak()` を使わないこと。** 読み上げ用の入口で**最短間隔 1.25秒**が
掛かっているので、続けて2匹出ると**声が落ちる回**ができる。
録音した1本を鳴らす `playVoice()` を通す（重なりは `voiceBusyUntil` が防ぐ）。

**同時に出た2匹が同じフレームで「ばあっ」と言わないこと。**
`voiceBusyUntil` に任せると2匹目が無言になるので、
**2匹目は 0.12秒ずらして鳴らす**（間隔があると「2匹出た」と分かる）。

**「いてっ」は痛そうにしないこと。** 声も見た目も**おどけた驚き**にする。
魚が痛がって悲しそうに見えると、1歳半には「叩いてはいけないこと」に見える。
目は閉じてよいが、**涙・青ざめ・×印の目は作らない**。

声は `scripts/make-voice.cjs` で WAV を base64 の `.ts` にする。
「ばあっ！」は**「ばあ！」で録ったものをそのまま持ってくる**。「いてっ」だけ新規。
**声は動的 import にすること**（「ばあ！」で静的に3本足したら
初期バンドルが 847KB → 1,023KB になった）。

### 4-5. 外したとき（不変条件3b）

魚の居ない水たまりを叩いた場合:

- 水面が**ぽちゃんと跳ねる**（しぶきを3粒、0.3秒）と、輪の波紋が広がる
- 低い音が鳴る（「ぽちゃ」）
- **得点は増えない**
- **声を出さない。** 「いてっ」も「ばあっ」も「あれ？」も鳴らさない。
  外れを失敗にしないし、**声は §4-4 の2つの意味だけに取っておく**

水たまりでも何でもないところを叩いた場合は、波紋と音だけ（不変条件1）。

### 4-6. やさしい得点

**人間が決めた**（2026-09-13）。得点は入れるが、**ゲームにはしない**。

- 叩いた数だけ、画面上部に**★が左から並ぶ**
- **10個たまると1行がまとめて花に変わり、★は 0 に戻る**。花は右へ積まれる
- **数字を使わない**（1歳半は読めない）
- **減らない。終わらない。制限時間が無い**（不変条件11）
- 花が10個たまったら…**何も起きない**。画面の端で折り返して並び続ける。
  「クリア」も「おめでとう」も作らない（終わりを作ると、そこで遊びが切れる）

**得点が増えるのは `rising` か `up` の魚を叩いたときだけ。**
`hit` 中の魚を連打しても増えない（増えると連打で無限に増え、
★が一瞬で埋まって意味を失う）。**ただし反応は返す**（不変条件2）——
潰れている魚を叩くと、ぷるっと跳ねて水の音が鳴る。
**「いてっ」は言わない**（2回言うと、2匹叩いたように聞こえる）。

### 4-7. 難しさの自動調整（介助であって、難易度ではない）

得点でも罰でもなく、**当たるようにするための介助**。

- 3回続けて叩けなかったら、`up` を 4.0秒 → **5.2秒**に伸ばす
- 5回続けて叩けたら、`up` を **3.2秒**に縮める
- 範囲は **3.2〜5.2秒**。これを超えて動かさない
- **画面に表示しない。** 子どもにも保護者にも見せない

**v0.1 の 2.0〜3.4秒から、まるごと後ろへずらした**（§4-1 の「ゆっくり」）。
**速い側の下限（3.2秒）を割らないこと。** 当たるようになってきたからと
速くしていくと、いちばん当たっていた設定を自分で壊す。

### 4-8. 岩陰のばあ（画面の上）

**2026-09-14、人間が決めた。** 「画面の上が空いているので岩を設置して、
タップすると魚が前に突き出してくるように（ばあっ！）してください。
suizokukan の下にある岩陰のばあの機能です」。

実測すると、いちばん上の水たまりの円の上端は Pixel 7 で画面 y = 293px にあり、
**その上の 293px（画面の 35%）に何も無かった。**

```
hidden ──(タップ)──> calling ──> popping ──> out ──┬──(叩かれた)──> hit ──> hidden
                                                   └──(時間切れ)──> retreating ──> hidden
```

| | 秒 |
|---|---|
| `callSec` 声だけ・姿を出さない | **0.38**（水たまりの魚と同じ） |
| `popSec` 前に突き出してくる | **0.42** |
| `outSec` 出たまま待つ | 2.60 |
| `hitSec` / `retreatSec` | 0.45 / 0.55 |

**抽選しない。** 押したときだけ出る（水たまりの魚は §4-2 で勝手に出る）。

#### みずのなかから変えたところが3つある

`creatures/HideoutSystem.ts` の移植だが、**この app の不変条件に合わせて
3箇所変えてある。** どれも単体テストが見張っている。

1. **「引っ込む途中は反応しない」を外した。** 向こうは `retreating` 中と
   `cooldown`（1.5秒）中のタップを捨てていたが、不変条件2 は
   「どの瞬間に押しても反応する。**アニメーション中だから無視は禁止**」。
   みずのなかの貝がまさにそれで壊れている（連打すると 0.45秒の開閉が
   一度も完了せず、**開き量の最大が 0.037** だった）
2. **隠れているあいだは `visible = false`。** 向こうは岩の形で隠していて、
   **面の隙間からウツボが透けていた**。ここは人間が
   「何も見えない状態で『ばあっ』と言って出てくる」と決めている（§v0.3）ので、
   形に頼らず消す。**確実で、速い**。
   ただし**待たせる場所は岩の中**にする（下の「岩の中で待たせる」）
3. **声が先、姿はあと**（`calling` 0.38秒）。向こうは出はじめと同時に鳴らす

#### 岩は2つ、魚は1匹

**押した岩から出る。** 2匹にすると同じ種が同時に2箇所へ出ることになり、
§4-2 の決まりを破る。魚が出ているあいだにもう一方の岩を押しても、
音と揺れは返る（不変条件1・3b。水たまりの空振りと同じ扱い）。

**岩の魚は、そのステージの水たまりに出ない種にする。**
モデルが3体しかないので、**もう一方のステージの魚を借りている**
（かわ の岩 = エイ／うみ の岩 = 琉金）。1ステージの3匹が同じ `.glb` を
2つ持つと「同じ魚を2色に塗った」に見えるため。

#### 絵を見て直した3つ

**どれも数値のテストは通っていた。**

- **岩が裂けて、割れ目から背景が明るい線として見えた。**
  `IcosahedronGeometry` は**索引を持たない**（実測: 42箇所の座標が
  240頂点に複製され、同じ角が最大6個）。**頂点の番号**でばらつかせると
  同じ角が別々に動く。**頂点の向き**で決めれば複製もそろって動く。
  みずのなかが「面の隙間から中身が透けていた」と書いているのも同じ話で、
  向こうはばらつきの幅を狭めて誤魔化している（0.78 → 0.90）
- **岩の下半分が真っ黒な塊になった。** 下を向いた面には主光も補助光も
  当たらないので、頂点カラーをいくら明るくしても黒いまま。
  弱い自発光（`emissive`）で底上げする
- **出きった魚の輪郭が岩に溶けた。** 倍率 1.45 だとエイが 1.58 × 1.80 になり、
  岩と同じ大きさの塊になる。**出ている途中（0.45）では良く見えていた**ので、
  「出はじめは良いのに出きると読めない」形だった。1.05 に下げ、
  岩を寒色の灰にして、魚は少し下がりながら出るようにした

#### 岩の中で待たせる（2026-09-14、人間の指示で直した）

**「岩から『ばあっ！』で出てくるのは前に突き出して出てくるような感じにして、
岩に隠れるようにしてください」。**

最初の版は魚を**岩の下端よりさらに下（開けた水の中）**で待たせていた。
`visible = false` で消してはいたので「見えない」は守れていたが、
出はじめの1フレームで**何もない水の中にぽっと湧いて**見えた。
「岩に隠れている」のではなく「消してある」だけだった。

直したこと:

- **待つ場所を岩の輪郭の中にした**（`HIDE_LIFT` 0.6 → 1.35）。
  岩の下端は y = 2.86 で、0.6 だと魚の下半分（2.855〜3.445）がはみ出していた
- **岩を縦長にした**（`Y_SQUASH` 0.88 → 1.15、`scale` 1.05 → 1.15）。
  魚が隠れられる高さが要る
- **岩の背中より奥から出す**（`HIDDEN_Z` −0.55 → −1.35）。
  岩の奥行きの半分は最大 0.76 で、中心が −0.35 なので背中は −1.11
- これで**深度で自然に隠れる**ので、`visible` を立てても1画素も出ない。
  岩を突き抜けて手前へ出てくる

#### 下げるのは演出ではなく、遠近の打ち消し

**手前へ出ると、画面の上では上へ動く。** カメラは原点を見ているので、
近づくほど画面の中心から離れる向きに広がる。岩は画面の中心より上にあるので、
**真っすぐ手前へ出すだけだと魚が上へ逃げる**。実測では出きった魚の中心が
画面 y = 113px（岩の下端は 178px）まで上がり、**岩の上に乗って**しまった。

倍率で言うと、隠れている z = −1.35 では 0.84、出きった z = +1.25 では 1.21。
**画面上の高さを変えないだけなら 1.10** で足りるが、それだと魚が岩の輪郭に
すっぽり収まったままで体の形が読めない。`DROP = 1.55` で少し余分に下げ、
**岩の下端をまたがせる**（上半分は岩に重なり、下半分は水に抜ける）。

#### 「隠れている」は位置ではなく遮蔽で確かめる

**カメラからレイを飛ばして、15×15 の格子で見る**（`tests/unit/safety.test.ts`）。
「ばあ！」で**高さが正しくても見えていない**ことが3回あり、逆に
**隠しているつもりで絵では隠れていない**こと（うみ の すいめん で
クマノミが丸ごと画面に出ていた）もあった。どれも数値のテストは通っていた。

格子は 15×15。「ばあ！」で 7×9 にしたら 0.05 幅の隙間をすり抜けている。

魚の代わりに**いちばん大きい魚（エイ 1.09 × 1.24 × 0.35）の箱**を置く。
node には DOM が無いので `createProceduralFish` は canvas の絵に落ち、
**実際より小さい板**になる。それで測ると甘い方向に外れる。

**最初の版はこのテストで 225点中 114点が見えていた。**

#### 岩は画面の上端で切る

**画面の中で完結する塊にしない。** 「岩が水に浮いている」に見える ——
「ばあ！」が隠れ場所を上下2段に置いて**上の段が空に浮き**、
見た目の判定が4場面で −12 を付け続けた（このアプリが §3-2 で
「水たまりは地平線より下にしか置かない」と決めた理由そのもの）。
上端を切らせると「画面の外へ続く岩場」に読めるので、浮かない。

#### 当たり判定は水たまりのぶんも見て縮める

岩は水たまりとは別の当たり判定なので、`HoleSystem.radiusAt()` は岩を見ていない。
`RockSystem.radiusAt()` が**水たまりの円を受け取って**、食い込まないところまで
縮める。実測（Pixel 7）: 岩 **108px / 103px**、いちばん上の水たまり 91px。

---

## 5. さかなと水たまり

### 5-1. データ構造

「ばあ！」の `SceneConfig` / `SpotConfig` の骨格は引き継ぐが、
**`AnimalConfig`（陸の動物25体）は使わない。** 魚は `FishConfig` を新しく作る。

```ts
export interface StageConfig {
  id: 'ike' | 'umi';
  name: string;                   // 「いけ」「うみ」
  backgroundUrl: string | null;   // peek-aboo の絵を2枚だけ使う
  sky: readonly [string, string];
  horizonV: number;               // 水たまりを置く基準（0〜1）。**手で入れる**
  water: readonly [string, string];  // 水面の色（浅い・深い）
  holes: readonly HoleConfig[];
  fish: readonly string[];        // このステージに出る魚（4種）
  flavor?: FlavorConfig;
}

export interface HoleConfig {
  id: string;
  position: readonly [number, number];  // ワールド x, y。地平線より下
  hitRadiusPx: number;                  // 上限。実際は radiusAt() が縮める
}

export interface FishConfig {
  id: string;
  name: string;                   // 「きんぎょ」など。画面には出さない
  color: number;                  // 体色
  accent: number;                 // 模様・ひれの色
  /** 輪郭。**ここを埋めないと全部同じ魚になる**（みずのなかの実測） */
  bodyHeight: number;             // 体高（体長に対する比）0.3〜1.0
  bodyWidth: number;              // 体幅（同）0.15〜0.5
  tail: 'fan' | 'fork' | 'round' | 'long';
  fins: 'small' | 'wide' | 'flowing';
  pattern: 'plain' | 'stripe' | 'spot' | 'band';
  /** 叩かれたときの潰れかた（§6-1） */
  squashStyle: 'flat' | 'wide' | 'spin';
  cutoutUrl: string | null;       // 絵を置いたら使う。無ければ手続き生成
}
```

**`SpotKind`（はこ・カーテン・とびら…）は要らない。** 水たまりは1種類でよい。
ふたを開ける動きも、中に動物を挟む前板・背板も、全部消える。
**`SpotShapes.ts` の 1,000行はほぼ捨てられる**（§7-3）。

#### 魚は4種だけ（1ステージ2種。2026-09-13、人間が決めた）

| id | 体高 | 体幅 | 尾 | ひれ | 模様 | ステージ |
|---|---|---|---|---|---|---|
| `kingyo`（きんぎょ）| **0.85** | 0.38 | fan | flowing | plain | いけ |
| `koi`（こい）| **0.50** | 0.26 | fork | small | spot | いけ |
| `kumanomi`（くまのみ）| **0.70** | 0.30 | round | wide | band | うみ |
| `fugu`（ふぐ）| **0.95** | 0.45 | round\* | small | spot | うみ |

\* ふぐの尾は `round` を小さく使う。**ふぐは叩くと膨らんでから沈む**（§6-2）。

**同じステージの2種は、必ず体高で 0.25 以上離すこと。**
きんぎょ 0.85 と こい 0.50、くまのみ 0.70 と ふぐ 0.95。
**色だけで分けないこと** —— 「みずのなか」で体型を指定しなかったら
チョウチョウウオもメダカも同じ魚になった。**見分けは輪郭で決まる。**

**2種に絞ったのは、種類を増やしても見分けきれないから。**
「ばあ！」は動物25体を出したが、それが面白さにつながった形跡は無かった。
**2種なら「どっちが出た」が分かる**ので、出るたびに意味がある。

#### 予備（採らなかった4種。消さずに残す）

`medaka`（0.40 / fork / small / plain）・`namazu`（0.45 / round / wide / plain）・
`tai`（0.75 / fork / small / plain）・`chouchouuo`（0.90 / fork / wide / stripe）。

**入れ替えたくなったときのために残す**（付録B の 7「魚が刺さっていない」）。
めだか は**小さすぎて叩きにくい**ので、入れ替えるなら体高を上げること。

### 5-2. 水たまりの形

水面を1つ作るだけ。「ばあ！」の隠れ場所9種の作り分けは要らない。

- **楕円の水面**（横長。見下ろし 11.8° のカメラから見て自然な比）
- ふちに**低い岸**（高さ 0.12）。魚が出ると岸の裏に腹が隠れる
- 水の中は暗い。ただし**真っ黒にしない**（背景より少し暗い程度）。
  `StageConfig.water` の2色で浅い→深いのグラデーションにする
- **魚は水面の奥（z < 0）から出て、手前（z > 0）には出ない。**
  「ばあ！」で踏んだ「板1枚の動物が前板より手前に出て隠れない」問題を、
  そもそも作らない
- **水面はゆっくり波打たせる**（1秒に3回を超える明滅を作らない。不変条件6）。
  魚が出ていないときも動いているので、画面が止まらない

**遮蔽のテストは残す。** `hidden` のとき、魚が1画素も見えていないこと
（15×15 の格子でレイを飛ばす。「ばあ！」のテストをそのまま使う）。
**格子は隙間の幅より細かく取ること** —— 7×9 では 0.05 幅の隙間をすり抜けた。

### 5-3. ステージは2つ、水たまりは各4箇所

**人間が決めた**（2026-09-13）。**7場面から2ステージに減らす。**

| ステージ | 背景 | 水たまり | 魚（**2種だけ**）|
|---|---|---|---|
| **あさせ** | **手続き生成の水中**（`scene/WaterScene.ts`）| 岩のくぼみ **6つ** | **きんぎょ・こい** |
| **うみ** | `うみ.webp` | 岩のくぼみ **6つ** | **くまのみ・ふぐ** |

**水たまりは6箇所、魚は2種**（2026-09-13、人間が決めた）。場所より種類を減らす。
叩く場所が多いと**どこを押しても当たりうる**ので、
1歳半には「押せるところが多い」ほうが効く（不変条件1・4c）。

**2列×3行に置く。** 3列×2行だと横の間隔が半分になって当たり判定が潰れる
（縦のほうが場所に余裕がある）。**行間は画面上で等間隔にすること** ——
ワールドの y で等間隔にすると、見下ろしのぶん下の行が画面上で詰まる。

**いちばん上の行を地平線まで上げない。** 上げれば当たり判定を 71.3px まで
稼げるが、遠くの丘に重なって**地平線に浮いて見えた**（実装して絵で確認した）。
それは §3-2 がこの設計の出発点にした欠点そのもの。草地まで下げてある。

実測の当たり半径: Pixel 7 **89.1px** / iPhone 12 **79.5px** /
360×600 63.4px / 横持ち 40.9px。**小さい端末と横持ちは 70px に届かない。**

- **同じ魚を2つのステージに出さない**（「ばあ！」の判断を引き継ぐ。
  輪郭が似ているだけでも同じに見える）
- **のはら（草原）の絵はやめた**（2026-09-14、実機で「背景が野原で魚と
  マッチしていない」と言われて）。「ばあ！」から持ってきた絵は7枚とも陸の
  場面で、**魚に合うのは うみ だけ**。2枚目は手続き生成で描く
  （`scene/WaterScene.ts`）。素材ファイルを増やさずに済み、不変条件7 も守れる
- **絵に穴を描き足さない**（絵と別に乗るので二重に見える。「ばあ！」の実測）
- **残り5枚の背景は使わない。** 消さずに `public/backgrounds/` に置いておく
  （ステージを足したくなったときのため）

#### ステージの切り替えかた

**画面に切替バーを置かない**（「ばあ！」の Phase 6 は未実装のまま終わった）。
1歳半にステージを選ばせる必要は無いし、**バーは当たり判定を塞ぐ**
（「みずのなか」で 390px 幅の端末で実際に起きた事故）。

**花が1つ咲いたら（＝★10個たまったら）ステージが切り替わる。**
- 得点が「場面が変わる」という形で返るので、★の意味が目に見える
- 切り替えは**待たせない。** 花が咲いた次の `hidden` で、静かに水面ごと入れ替える
- **切り替え中も叩ける**（不変条件2）。暗転やローディング画面を作らない
- 2ステージしか無いので、交互に行き来するだけ

**`__poko.setStage('umi')` を残す**（開発と E2E 用。「ばあ！」の `__peekaboo` と同じ）。

---

## 6. 飽きさせない仕組み

「ばあ！」の §6 を、たたき用に読み替える。**入れる順は 小 → 中 → 大。**

### 6-1. 毎回変わるもの（小）

- 出る速さ ±10%（`rising` の 0.65秒を振る）
- 大きさ ±10%
- 音と声の高さ ±5%（「ばあっ」「いてっ」のピッチ）
- **潰れかたを3種類**（`FishConfig.squashStyle`: 縦に潰れる／横に伸びる／くるっと回って沈む）
- 水面から出る角度 ±8°（まっすぐ上だけだと並んで見える）

**合計時間は変えない。** 「ばあ！」で速さを振ったら §4-3 の表とずれて
安全テストが5件落ちた。速くなったぶんは待ちで吸収する。

### 6-2. ときどき変わるもの（中）

- **大きいさかな**（**20回に1回**。2026-09-14、人間が決め直した）。1.4倍で、
  叩くと星が3つ増える。出ている時間は同じ。
  **前は 5回に1回と決めていたが、実機で「出現頻度が高い」と言われた。**
  ただし**そのとき出ていたのは大きいさかなではない**（この機能はまだ
  入っていない）。魚の大きさを体高でそろえていたので、細長い こい だけが
  幅 0.63・ふぐ 0.42 と 1.5倍に描かれていて、それが「大きい魚」に見えていた。
  **面積でそろえて直した**（`ProceduralFish` の `TARGET_AREA`）。
  そのうえで「大きいさかな」は**レアな要素**にする。
  **`BIG_CHANCE` は抽選の当たりであって、出る割合ではない。**
  2回続けて大きいのを出さない規則を入れるなら、**実測が 5回に1回になるまで
  当たりを上げること**（「ばあ！」でサプライズに 1/3 をそのまま入れたら、
  実測は 3.7〜4.0回に1回だった）。**必ず数えて確かめる**
- ~~2匹が同時に出る（4回に1回）~~
  → **削除。** 同時に出る上限が2匹になったので（§4-2）、
  2匹はもう特別な出来事ではない。**両方叩けたら星が2つ増える**のはそのまま
- **きんいろのさかな**（16回に1回）。叩くと花が1つ増える＝**ステージが変わる**（§5-3）
- **ふぐは叩くと膨らんでから沈む**（`squashStyle` とは別の、種ごとの癖）。
  「ばあ！」で「出かたの癖は出たあとに見せる」と学んだのと同じで、
  **叩いた 0.12秒の潰れには足さず、そのあとの 0.33秒で見せる**
- ステージごとの味つけ（「ばあ！」の `Flavor.ts` を縮小して引き継ぐ）:
  ゆれる草（いけ）／あぶく（うみ。押すと割れる）／横切る小魚

### 6-3. たまに起きるもの（大・サプライズ）

「ばあ！」の `Surprise.ts` を引き継ぐ。**ただし出方は上下だけ**
（左右は「ばあっぽくない」と言われて 2026-09-07 に取り下げた）。

- **画面の下から大きく1匹**。叩くと星が5つ
- 頻度は「ばあ！」と同じく**3回に1回**から始め、飽きが早ければ下げる。
  `SURPRISE_CHANCE` は抽選の当たりであって出る割合ではない
  （2連続で出さないぶん下がる。1/3 をそのまま入れた実測は 3.7〜4.0回に1回）
  → **実装では「抽選」ではなく「時計」にした**（2026-09-17。下を読むこと）
- **大きいぶん、ゆっくり出す**（`rising` 0.65秒 → 0.9秒）。
  大きいものが速く出ると驚かせるだけになる

**実装で変えたこと（2026-09-17）:**

- **抽選ではなく時計で出す（22〜34秒に1回）。** 「3回に1回」は「ばあ！」の
  **タップに紐づく**抽選だった。ぽこ！の魚は押さなくても出続けるので、
  出現数に紐づけると**よく叩く子ほどサプライズが増えて**、
  「大きいのが普通」になる（§6-3 が避けたいことそのもの）。
  時計なら遊びかたに依らない。飽きが早ければ `SURPRISE.minGapSec` を上げる
- **岩の魚が隠れているときだけ始める。** 魚のモデルは3体しかなく、
  サプライズは岩と同じ種を使うので、同時に出ると「同じ魚が2箇所」になる
- **出ているあいだ、水たまりに新しい魚を出さない**（`Spawner.setHeld()`）。
  サプライズは画面のいちばん前に出て**下の段の水たまりを覆う**
  （実測 412×839 で、下の段の芯は当たり楕円の内側 0.32 / 0.63）。
  止めないと、覆われて見えない魚がそこに出て
  「押した相手と違うものが反応する」になる。
  **止める旗は入れ替え待ち（§5-3）と別に持つ** —— 1つにすると、
  サプライズが終わった瞬間に入れ替え待ちまで解ける
- **当たり判定は円ではなく、見かけの矩形に内接する楕円。**
  絵に重ねて測ったら、エイ（横 302px・縦 224px の平たい魚）の
  **右の翼が円の外**にあった（見えているのに叩けない）。
  矩形は体の世界の境界箱を投影して求めるが、**奥行きは中央だけで測る** ——
  8隅すべてを投影すると、カメラに近い面の角が大きく写って
  縦 448px（見えている体の2倍）にふくらむ。
  四角いまま使わないのは、角が水しか無いのに★が5つ入るため
  （「ばあ！」の「板は四角いのでレイは透明な角にも当たる」と同じ事故）

---

## 7. 技術スタックと、peek-aboo からの再利用

### 7-1. スタック

TypeScript (strict) / three.js / Vite / vite-plugin-pwa。**変えない。**

- React・その他UIフレームワークを入れない
- `any` を使わない
- 新しい依存を安易に増やさない。手続き生成で済むものは自前で作る

### 7-2. そのまま持ってくるもの（**実機検証済み。書き直さない**）

| 持ってくるもの | 中身 |
|---|---|
| `src/core/` 全部 | Loop（固定タイムステップ）/ Input / ScreenProjector / AssetLoader / AudioBus / QualityManager / WakeLock |
| `src/ui/` 全部 | ParentalGate（右上24px・2秒長押し）/ Ripple / styles.css |
| `src/pwa/` | Service Worker 登録 |
| `src/scene/Backdrop.ts` | 背景の絵・落ち影・**地平線の検出**・場面の光の色と向き |
| `src/peekaboo/CutoutAnimal.ts` | 絵を板に貼る（道A）。`poko/` に移す。**魚の絵を置いたとき用** |
| `src/data/look.ts` | 見た目の調整値（`FORM_SHADING` ほか） |
| `public/backgrounds/` のうち **のはら・うみ の2枚** | **作り直さない** |
| `public/voice/baa.*` | 「ばあっ！」の録音。**そのまま使う**（§4-4） |
| `scripts/` | `make-backgrounds` / `make-voice` / `visual-shot` / `visual-score` |
| `docs/visual-gate.md` / `.claude/agents/visual-judge.md` | 見た目の採点（§11-4） |
| `tests/` の作法 | レイで遮蔽を見る／壁時計で待たない／画素比較しない |

**ファイル冒頭に出どころを書くこと。** 「ばあ！」から持ってきたものは
実機で検証済みなので、理由なく書き直さない。

### 7-3. 捨てるもの

| 捨てるもの | 理由 |
|---|---|
| `SpotSystem.ts` | 状態遷移が別物。`HoleSystem` + `MoleSystem` に作り直す |
| `SpotShapes.ts` の大半 | 隠れ場所9種の作り分けが要らない。水たまり1種だけ残す |
| `ChaseSystem.ts` | 移動モードは無い |
| `EmptySpot.ts` | 空振りの扱いが §4-5 に変わる |
| `SpotShuffle.ts` | 魚は毎回抽選されるので要らない |
| `AnimalSystem.ts` の「隠す」部分 | ヒント（`hintPart` のはみ出し）の計算が丸ごと不要 |
| サプライズの左右 | 2026-09-07 に取り下げ済み |
| `src/data/animals.ts`（陸の動物25体）| **魚にしたので使わない。** `FishConfig` を新しく作る（§5-1）|
| `src/peekaboo/ProceduralAnimals.ts` | 同上。`ProceduralFish.ts` を新しく書く |
| `public/animals/` 25枚 | 同上。**消さずに置いておく**（「ばあ！」に戻したくなったとき用）|
| 背景 5枚（おうち・そと・のうじょう・どうぶつえん・きょうりゅう）| 2ステージにしたので使わない。**消さずに置いておく** |
| `docs/match-gate.md` と `npm run match-score` | 参照画像に寄せるゲート。**魚は参照画像を持たない**ので比べる相手が無い |

### 7-4. 新規に書くもの

```
src/poko/
├── HoleSystem.ts      水たまりの生成・配置・当たり判定（画面座標）
├── FishSystem.ts      さかなの状態遷移・叩かれた反応
├── Spawner.ts         出現の抽選（同時数・間隔・偏りの回避・介助）
├── HitEffect.ts       潰れ・しぶき・ゆれ（「ばあ！」で未実装だった RevealEffect の実体）
├── Score.ts           やさしい得点（★と花）と、花によるステージ切替
├── WaterShape.ts      水たまりの手続き生成（波打つ水面を含む）
├── ProceduralFish.ts  魚の手続き生成（`FishConfig` から輪郭を作る）
└── CutoutFish.ts      ★ CutoutAnimal の移植。魚の絵を置いたとき用

src/data/
├── stages.ts   2ステージの定義（データのみ。ロジックを置かない）
├── fish.ts     魚4種の定義（同上。予備の4種はコメントで残す）
└── look.ts     ★ 移植
```

---

## 8. 見た目

### 8-1. 背景はそのまま（2枚だけ使う）

`public/backgrounds/` の **のはら・うみ の2枚**をそのまま使う。**作り直さない。**

- 1024 × 2048（縦 1:2）。この奥行き（z = -1.55）で画面に入るのは縦 11.36・横 5.57
- **光を当てない**（`MeshBasicMaterial` ＋ `toneMapped = false`）
- 場面の光の**色**は絵の上半分・下半分の平均から、**向き**は
  絵のいちばん明るいマスから読む（`Backdrop.ts` に実装済み）

### 8-2. 「ばあ！」で実測して分かったこと（水たまりにも効く）

- **平らな面は、光を足しても明るさが一様のまま。** 平行光も半球光も法線しか
  見ないので、板1枚の上ではどこも同じ明るさになる。頂点カラーで焼き込む
  （`FORM_SHADING`: 上を明るく下を暗く、光の側を明るく、奥ほど暗く）
- **縞や斑点で明暗の幅を稼いでも、面は平らに見える。** 面の向きに沿った勾配が要る
- **手続き生成の模様は、白から描き始めない。** 白に白を重ねても変わらないので、
  中間の灰から描く。暗くなるぶんは線形空間の平均を測って 1/平均 倍で戻す
- **丸い塊に四角い板を1枚でも混ぜると、そこだけ人工物に見える。**
  水たまりの縁は楕円にし、塞ぐための平らな板を混ぜない
- **落ち影は上の段と下の段で見かけの上下がひっくり返る。** 影は奥にあるので
  カメラから見て画面中心へ寄る。見かけの位置で指定してローカル座標に逆算する
- **影を濃くして「接地」を稼がない。** 背景まで暗くして、輪郭がかえって消える
- **空には影を落とさない。** 水たまりは地平線より下にしか置かないので、
  「ぽこ！」では原理的に起きない

### 8-3. 見た目の採点を引き継ぐ

`docs/visual-gate.md` と `visual-judge`（`.claude/agents/`）をそのまま持ってくる。

```bash
npm run visual:shot -- --out baseline   # 改善前を撮る
npm run visual:shot                     # いまを撮る
npm run visual:score -- --dir current --vs baseline
```

**CI の合否には使わない**（§11-2）。人間と `visual-judge` が採点するための材料。
**閾値を自分で緩めない**（人間が決める）。

**「ばあ！」で学んだこと**: V1（輪郭の明度差）と V6（消える輪郭の割合）は
L\* だけを見るので、色相だけで分かれている組を落第にする。
しかも**形に沿った勾配を強くするほど隠れ場所の明暗の幅が広がり、
輪郭のどこかが必ず背景と同じ明るさになる**。
「面を立体に見せる」と「輪郭をはっきりさせる」は同じ方向ではない。
**「ぽこ！」では V1/V6 の閾値を最初から人間に決めてもらう。**

---

## 9. 素材が無くても動く（不変条件7）

`public/` を空にしても `npm run dev` が動くこと。

| 素材 | 無いときの代役 |
|---|---|
| 背景の絵 | 手続き生成のグラデーション（`StageConfig.sky` の2色） |
| 魚の切り抜き | `ProceduralFish`（`FishConfig` の体高・尾・ひれ・模様から輪郭を作る）。**こちらが既定** |
| 水たまり | もともと手続き生成 |
| 音 | WebAudio で合成 |
| 声（「ばあっ」「いてっ」）| 鳴らさない（例外を投げない）。**画面の反応だけで遊びは成り立つ** |

**素材のURLは必ず `resolveAssetUrl()` を通す。** 通っていないと、
フォールバックが効いて「一応動く」ので、素材を置いた日まで気づけない。

---

## 10. パフォーマンス

### 10-1. 目標

| 端末 | 目標 |
|---|---|
| iPhone 12 / Pixel 5 以降 | 60fps |
| 廉価 Android | 30fps を下回らない |

「ばあ！」の実測（三角形 / draw call）:

| 場面 | 三角形 | draw call |
|---|---|---|
| おうち | 800 | 35 |
| そと | 4,162 | 59 |
| のはら | 8,800 | 96 |

**「ぽこ！」は「ばあ！」より軽くなるはず** —— 隠れ場所9種の作り分けが消えて
水たまり1種になり、**場面も7つから2つに減る**ため。
増えるのはしぶき（`HitEffect`）と、波打つ水面だけ。

### 10-2. fps は CI の合否にしない

開発コンテナにも CI にも GPU が無くソフトウェア描画なので、実機性能を反映しない。
**「フレームが進んでいること」だけを見る。**

### 10-3. 毎フレーム走るコードで new をしない

使い捨てのベクトルはモジュール定数に置く。

### 10-4. 形を足すときは draw call も数える

「ばあ！」の実測: 12×8 の球を7つ**別メッシュで**足したら、
三角形が +30%・draw call が +12% になった（予算は +10%）。
分割を落とし、**複数を1つのジオメトリに結合**して収めた。
星も同じ —— **1つのジオメトリにまとめて出す**。

---

## 11. テストと受け入れ条件

### 11-1. 3層

| 層 | 何を見るか |
|---|---|
| 単体（Vitest・node） | 不変条件。状態遷移・得点・抽選・遮蔽（レイ） |
| E2E（Playwright） | 実際のブラウザでの操作。当たり判定・UI の重なり・ステージ切替 |
| 見た目（`visual-judge`） | **合否ではなく採点**。人間が最後に決める |

### 11-2. CI が判定しないこと

- **fps の数値**（GPU が無い）
- **動いている場面のスクリーンショットの画素比較**（必ず落ちる）。
  例外は `npm run shot -- --pose reference` の静止1枚のみ。
  ただし**当たらないことを毎回証明する**（同じコードで2回撮ってバイト一致）
- **見た目が良いかどうか**（人間が実機で判断する）

### 11-3. E2E で必ず見ること

- 画面のどこを押しても波紋が出る（右上 24×24px を除く）
- **叩いた同じフレームで潰れが始まる**（0フレーム原則）
- 連打しても1回ごとに反応が返る
- 得点表示の下に当たり判定が無い
- 水たまりどうしの当たり判定の距離（端末4種）
- **ステージを10往復しても** `renderer.info.memory` が増え続けない
- **叩ける相手が 0 になる時間が無い**（10秒ぶん回して確かめる）
- **ステージ切替の最中に叩いても反応が返る**（不変条件2）
- **同じステージの2種が輪郭で見分けられる**（シルエットの重ね合わせ IoU が 0.80 を超えない）。
  「ばあ！」で岩とくさむらが IoU 0.789〜0.879 で「同じ塊を2色に塗っただけ」と
  判定に言われた。**色を変えても輪郭が同じなら同じに見える**

### 11-4. 「ばあ！」で踏んだ、テストそのものの罠

**すべて実際に起きた。同じ失敗を繰り返さない。**

- **`npx vitest run | tail -3` では要約行が隠れる。** これで単体テストが
  1件落ちたまま16サイクル進んだ。`grep -E "Tests "` で要約を必ず見る
- **待つ長さを壁時計で決めない。** `Loop` は1フレームに最大3ステップしか
  進めないので、GPU の無い環境では壁時計と更新時計が2倍近くずれる。
  見た目を重くするたびに壁時計の待ちが足りなくなって、
  **通し実行のときだけ落ちる**テストになった（3箇所で起きた）。
  `getSimulatedSeconds()` で待ち、壁時計は固まったときに止めるためだけに使う
- **`renderer.info.memory` は数え終わる前に測ると「漏れている」と出る。**
  数が2回続けて同じになるまで描いてから測る
- **「置いたのに見えない」は、まず遮蔽を疑う。** 赤く塗って撮る
- **撮影した PNG を git に入れない。** 毎サイクル差し替わるので
  `.git` が 29MB → 81MB に膨らんだ。baseline だけ残す

---

## 12. 実装フェーズ

**素材ゼロで動くところから始める**（不変条件7）。

### Phase 1: 移植と骨組み（素材ゼロで動く）
- `core/` `ui/` `pwa/` `scene/Backdrop.ts` を peek-aboo から移す
- `data/look.ts` を移す
- `HoleSystem` + `WaterShape`。水たまりが4つ並び、押すと波紋が出る
- 単体テスト: 当たり判定の距離、素材ゼロで起動

### Phase 2: さかなが出入りする
- `data/fish.ts` + `ProceduralFish`。**同じステージの2種が輪郭で見分けられること**
- `FishSystem` + `Spawner`。押さなくても、**ゆっくり**出入りする
- **不変条件4c**（叩ける相手が途切れない）のテストを先に書く

### Phase 3: 叩く（**ここが山場**）
- `FishSystem.hit()` と **0フレーム原則**
- `HitEffect`（潰れ・しぶき・ゆれ）
- 音（「ぽこっ」）と声（**出るとき「ばあっ！」／叩くと「いてっ」**）
- **ここまでで一度、実機で子どもに触らせる。** 面白がるかどうかが
  このアプリの全部なので、先に確かめる。
  **ここで受けなければ、先に進まずに設計へ戻る**（付録B）

### Phase 4: 得点と2ステージ ✅（2026-09-15）
- `Score`（★と花）。減らない・終わらない
- **花が咲いたらステージが変わる**（§5-3）。暗転もローディングも作らない
- 得点表示が当たり判定を塞がないことを E2E で見る

実装して分かったこと:

- **「最後の1匹は代わりが出るまで沈まない」（不変条件4c）は、入れ替えの
  邪魔をしない。** 抽選を止めたら最後の1匹が出たまま止まるのでは、と思って
  解除する仕掛けを書いたが、**8通りの時点で止めて測ったら7通りで
  フレーム数がまったく同じ**だった（残り1通りも 63 → 32フレーム）。
  あの決まりが効くのは「相棒が沈んでいる最中」だけで、相棒はすぐ `hidden` に
  なるので自然に解ける。**要らない機構は置かない**ので外した。
  実測の待ち時間は **32〜283フレーム（0.5〜4.7秒）**
- **得点表示は `pointer-events: none` を外さないこと。** ★の帯は
  **画面の上の岩（§4-8）の当たり判定に重なる**（岩の円は画面 y 80〜296px）。
  塞いだ瞬間に岩のばあが死ぬ。E2E が「★の上を押しても反応が返る」
  「★を並べたあとでも岩が押せる」の2つで見張っている
- **岩の魚を叩いたぶんも★に数える**（§4-8 は Phase 4 より後に足したので
  §4-6 には書いていない）。1匹は1匹

### Phase 5: 素材を入れる
- 背景 のはら・うみ の2枚を `public/backgrounds/` に置く
- 見た目の採点（`visual:shot` / `visual:score`）の baseline を撮る。
  **2ステージなので、「ばあ！」の7場面より採点はずっと軽い**

### Phase 6: 飽きさせない仕組み（§6-2 の2つだけ入った。2026-09-16）
- §6-1（小）→ §6-2（中）→ §6-3（大）の順
- **1つ入れるたびに実機で見る**

**人間が §6-2 から2つ選んだ**（2026-09-16）: 大きいさかな・きんいろのさかな。
**§6-3（サプライズ）も入った**（2026-09-17）。§6-1（小）は未着手。

実装して分かったこと:

- **抽選は乱数を1つだけ引いて、数直線を3つに区切る。**
  先にきんいろを判定して残りで大きいを判定すると、
  **大きいの実測が 1/20 ではなく 1/17.1 になる**（きんいろを外したぶん
  出やすくなるため）。2連続を禁じる規則は入れていないので、当たり＝実測。
  **実測: きんいろ 16.1回に1回・大きい 20.5回に1回**（5,280回ぶん数えた）
- **色を「掛ける」だけでは金にならない**（絵で確認した）。材質の `color` は
  テクスチャや頂点カラーに**掛け算**で効くので、赤い金魚に金を掛けても
  橙のままで、青いハギに掛けると暗い緑になる。CLAUDE.md の
  「白の上に白を重ねても何も変わらない」と同じ話。
  **体の絵と頂点カラーごと外して、まっさらな金に置き換える**（`setGold`）。
  輪郭は残るので「金色になった魚」に見える。
  canvas の絵のほう（不変条件7 のフォールバック）は `map` を外すと
  **金色の四角い板**になるので、そこだけ弱い金で妥協している
- **引っ込んだら色と大きさを必ず戻す。** 魚は1種につき1匹しか持っていないので、
  戻し忘れると次に出たときも金のまま・大きいままになる
- **1.4倍は、隣に比べるものが無いと分かりにくい**（高さの差は 17〜19px）。
  数字は設計どおりにしてあるので、**上げるなら人間が決めること**
- `__poko.forceVariant('gold')` で次の1匹を決め打ちできる。
  **16回に1回を実機で待つのは現実的でない**ため

### Phase 7: 仕上げ
- `prefers-reduced-motion`
- ペアレンタルゲートの奥に音量とステージのリセット

**「ばあ！」の Phase 7（画面上の切替バー）は作らない**（§5-3）。
ステージは花が咲いたときに変わるので、**押すところを増やさない。**

---

## 13. 公開

Netlify（`netlify.toml`）。`main` に push すると自動で公開される。

**無料プランは右下に「Powered by Netlify」のバッジを挿入する。**
バッジはリンクなので、押すと外部サイトに飛ぶ（不変条件10 に触れる）。
気にするなら Cloudflare Pages（無料・バッジなし）に移す。

---

## 14. 引き継ぐ教訓（実測値つき）

**すべて実測値。同じ失敗を繰り返さないために残してある。**

### 14-1. 「連打しても壊れない」は「連打しても動く」まで確かめる

みずのなかの貝は開閉の途中でタップされると向きを反転していたため、
連打すると 0.45秒の開閉が一度も完了せず、**開き量の最大が 0.037**だった
（1回押しなら 1.0）。実機で「触っても反応しない」と報告された。

**例外を返して何もしないのは、無反応を作るのと同じ。**

### 14-2. 当たり判定は画面座標で。置いたら距離を測る

岩の裏や真横から当たりが出ず「押したのに反応しない」が起きた。
カニとエビが 0.16m 差で置かれ、実機で重なって見えた。
**目で見て気づけない。**

### 14-3. 「大きくすれば見える」は成り立たないことがある

生き物を 2.5倍にしても、判定に使っていた指標は 0.016% から1ミリも動かなかった。
**指標が動かないときは、対象ではなく指標の設計を疑う。**

### 14-4. オブジェクトを1つ足すと、無関係な測定値まで動く

three は `generateUUID()` で1オブジェクトにつき `Math.random()` を4回消費する。
モデルを1つ足しただけで乱数列がずれ、**触っていない水槽の測定値**が
0.980 → 0.173 に動いた。**遊びの乱数は独立したシードから引く。**

### 14-5. `Input` は `document.body` に付ける

`#overlay-layer` は `pointer-events: none` なので、そこに付けると
タップが1度もハンドラに届かない。**不変条件1 が丸ごと死ぬ。**

### 14-6. UI が当たり判定を塞ぐ事故

切替ボタンを 88px × 5個 並べたら 390px 幅の端末で2行に折り返し、
バーの高さが 172px になって画面下部の当たり判定に覆いかぶさった。

### 14-7. 「声が鳴らない」は、たいてい間隔制限

`AudioBus.speak()` は読み上げ用の入口で、最短間隔 1.25秒 が掛かっている。
連打される音は `playOneShot()` / `playVoice()` を通すこと。

### 14-8. 加算で足す光は、体色を消す

彩度 0.70 のクマノミが淡いサーモンピンクにしか見えなかった原因は、
生き物に乗せていた加算の光（最大 +1.7）だった。
**「色が出ない」ときは、素材や `color` を触る前に加算項の強さを確かめる。**

### 14-9. 「ばあ！」そのものが、いちばん大きな教訓

**面白さは、実機で子どもに触らせるまで分からない。**
「ばあ！」は不変条件も、テストも、見た目の採点も揃っていて、
それでも **1〜2回触られただけで終わった**。

だから今回は、**Phase 3（叩く）が終わった時点で一度止めて、実機で確かめる**。
そこで面白がらなければ、得点も飾りも作らない。

---

## 付録A: 「ばあ！」で人間が決めたことのうち、「ぽこ！」で反転するもの

| 「ばあ！」での決定 | 「ぽこ！」では | 理由 |
|---|---|---|
| **1体ずつしか出さない**（2026-09-07） | **反転。同時に複数出す** | 「ばあ！」は4体同時だと「自分が押したから出た」が読めなかった。たたきは**叩いた相手が潰れる**ので、因果は触った場所で読める。むしろ1匹ずつだと叩く相手が無い時間ができる（不変条件4c に反する） |
| **ヒント＝体の一部を出す**（不変条件3） | **削除** | 魚は全身を出す |
| **ため（0.35秒）** | **削除** | いないいないばあの核だったが、たたきでは因果を切る |
| **動物25体を出す**（種類が価値） | **魚4種にする。1ステージ2種**（2026-09-13） | **本人が魚を好きだから。** そして**種類の多さは価値ではなかった** —— 25体出しても面白さにつながった形跡が無い。2種なら「どっちが出た」が分かる |
| **場面7つ**（2026-09-06 に5→7に増やした） | **2ステージにする**（2026-09-13） | 場面を増やしても遊びは深くならなかった。**切り替えずに遊び続ける**ほうが合う |
| **場面切替バー**（Phase 6 として未実装のまま） | **作らない** | 押すところを増やさない。ステージは花が咲いたときに変わる（§5-3） |
| **「みんなで鳴く」に「ばあっ！」を使わない**（2026-09-07） | **「ばあっ！」を主役に戻す**（2026-09-13） | 禁じた理由は「隠れたままなのに『ばあっ』と言うのが紛らわしい」だった。**魚は本当に出てくる**ので、ここでは正しい合図になる |
| **登場は 0.35秒以内に始まる**（不変条件4） | **0秒（叩いた反応）と 0.65秒（出てくる動き）に分かれる** | 速くすべきなのは**押した結果**だけ。出てくる動きは**むしろ遅いほうが当たる**（§4-1） |
| **のはら だけ5箇所**（2026-09-05） | **4箇所に揃える** | 移動モードが無くなり、5箇所にする理由が消えた |
| **サプライズは上下だけ**（2026-09-07） | **そのまま引き継ぐ** | 左右は「通り過ぎる」に見えた |
| **たまごが隠れ場所に入れ替わる**（2026-09-07） | **削除** | 隠れ場所の入れ替えという概念が無い |
| **参照画像に寄せるゲート**（M1・M2・M5） | **削除** | 魚は手続き生成で、比べる参照画像を持たない |

## 付録B: この設計が失敗したと分かる条件

**実機で子どもが、叩いたあと2回目を叩かなければ失敗。**

そのときに疑う順番:

1. **反応が遅い** → `hit()` から画面に出るまでを測る。16ms を超えていないか
2. **反応が小さい** → 潰れ・星・ゆれのどれかが効いていない。赤く塗って撮る
3. **叩く相手が居ない** → `up` か `rising` の数が 0 になる時間を測る
4. **当たらない** → 当たり判定の半径を測る。`radiusAt()` が縮めすぎていないか
5. **音が鳴っていない** → `speak()` の間隔制限に掛かっていないか（14-7）。
   **`playVoice()` を通しているか**（§4-4）
6. **出るのが速すぎる／遅すぎる** → `up` を測る。介助が 3.2〜5.2秒の外に出ていないか。
   **速すぎて当たらない**のと**遅すぎて待てない**のは、見た目が同じ（どちらも触らなくなる）。
   `up` を 3.2秒と 5.2秒に固定した2版を作って、どちらで触るかを見る
7. **魚が刺さっていない** → 4種の輪郭を並べて見せ、**どれを指さすか**を見る。
   指さす魚が1匹も無ければ、予備の4種（§5-1）と入れ替える。
   **種類を増やして解決しようとしないこと** —— 25体出した「ばあ！」が
   それで受けなかった

**ここまで潰しても2回目が無ければ、「叩く」自体が刺さらなかったということ。**
そのときは作り込みを続けず、人間に報告して止める。
**「ばあ！」で、受けないものを作り込み続けたのがいちばんの損失だった。**
