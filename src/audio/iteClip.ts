/**
 * 叩かれたときの声「いてっ」（設計書 §4-4）。
 *
 * ==========================================================================
 * **まだ録っていない。** Phase 3 で `scripts/make-voice.cjs` が
 * WAV を base64 にして、この定数を埋める。
 *
 * それまでは**空文字のまま**にしておく。`AudioBus` は
 * `decodeAudioData` が失敗したら false を返して合成音に落ちるので、
 * **声が無くてもアプリは動く**（不変条件7）。例外は表に出ない。
 *
 * **「いてっ」を痛そうにしないこと**（不変条件11b）。おどけた驚きにする。
 * 魚が痛がって悲しそうに聞こえると、1歳半には
 * 「叩いてはいけないこと」に見える。
 * ==========================================================================
 */
export const ITE_WAV_BASE64 = '';
